To use a digit d (in character form) as a subscript into the array a, we would
write `a[d-'0']`.

This assumes that digits have consecutive codes in the underlying character set,
which is true of ASCII and other popular character sets. 
